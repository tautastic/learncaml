(*
   Challenge: Alpha Range
   Write a function alpha_range which takes three arguments start, stop, and step (default 1)
   and returns a list of alphabetical characters from start to stop inclusive, with step direction;
   handles errors.
*)

let alpha_range start stop step =
  failwith "not implemented"

